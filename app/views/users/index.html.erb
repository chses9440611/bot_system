<p id="notice"><%= notice %></p>

<h1>Booking System</h1>

<table>
	<thead>
		<tr>
			<th>Name</th>
			<th>Student ID</th>
			<th></th>
		</tr>
	</thead>

	<tbody>
		<% @users.each do |user| %>
			<tr>
				<td><%= user.name%></td>
				<td><%= user.student_id %></td>
				<td><%= link_to 'Cancel', user, method: :delete, data:{ confirm: "Are you sure to cancel?"} %></td>
			</tr>
		<% end %>
	</tbody>
</table>

<br>

<%= link_to 'Book washer', new_user_path %>
